# Hugging Face Cache

Optional offline cache for transformer checkpoints and sentence transformer models. Keep this path distinct from `/srv/frappe-llm/output` to avoid clashes with Axolotl artifacts.
