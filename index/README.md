# Embedding Indexes

Build retrieval or search indexes here using `scripts/build_index.py`. Outputs from this script should never be placed in `/srv/frappe-llm/output` or `/srv/frappe-llm/prepared` to keep Axolotl runs isolated.
