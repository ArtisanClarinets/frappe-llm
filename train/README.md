# Training Artifacts (Placeholder)

Keep this directory empty in git. Real checkpoints are written to `/srv/frappe-llm/output/qwen25-coder-3b-frappe-qlora` by the Axolotl wrappers in `scripts/train_sft.py`.
