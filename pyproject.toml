[project]
name = "frappe-llm"
version = "0.1.0"
description = "Frappe LLM fine-tuning, DPO alignment, and evaluation utilities"
authors = [{ name = "Frappe" }]
requires-python = ">=3.10"
readme = "README.md"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["scripts"]
addopts = "-q"
