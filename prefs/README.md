# Preference Data (Legacy)

Pairwise preference logs for alignment experiments live here. When useful, they can be down-selected and converted into messages using `scripts/convert_to_messages.py --pairs-jsonl prefs/frappe_pairs.jsonl`.
