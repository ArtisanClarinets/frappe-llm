# Training Virtual Environment

Record pinned dependencies for the primary Axolotl training environment here if you snapshot them. The repository ships with instructions in `README.md` instead of tracking binaries.
