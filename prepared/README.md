# Axolotl Prepared Datasets (Placeholder)

Axolotl writes tokenised datasets into `/srv/frappe-llm/prepared`. This directory exists solely as a reminder of that external path and should remain empty in git.
